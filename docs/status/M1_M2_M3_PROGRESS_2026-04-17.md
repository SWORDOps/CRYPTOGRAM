# M1/M2/M3 Progress Update (2026-04-17)

This checkpoint captures current build-parity progress for Linux and Android against the optimistic roadmap.

## Scope

- Branch: `dev`
- Goal: converge on full Linux + Android feature/build parity (slow and steady, feature-complete target)
- Baseline reference: ongoing parity work against upstream Telegram Desktop APIs and dependency drift

## M1 (Linux Desktop Parity)

Status: **In Progress**

### What advanced in this pass

- Resolved multiple Qt/UI API drift breakages in Telegram Desktop sources, including:
  - menu parent type migrations (`PopupMenu*`/`RpWidget*` -> `Ui::Menu::Menu*` paths)
  - removed legacy hooks (`initResizeHook` -> `fitToMenuWidth` usage in affected custom menu items)
  - migrated old `RoundButton::TextTransform` references to `Ui::RoundButtonTextTransform`
- Build progression increased materially vs prior checkpoint (moved from earlier low-30% failure points to later translation units in current pass).

### Current blocker

- Build currently pauses at further reactive API compatibility in:
  - `Telegram/SourceFiles/ui/chat/group_call_bar.cpp`
- Error class: missing `rpl::start_with_next_done` symbol in current `rpl` surface visible to this tree.

### Key changed files in this checkpoint (non-exhaustive)

- `Telegram/SourceFiles/media/player/media_player_dropdown.cpp`
- `Telegram/SourceFiles/menu/gift_resale_filter.h`
- `Telegram/SourceFiles/menu/gift_resale_filter.cpp`
- `Telegram/SourceFiles/menu/menu_item_rate_transcribe.cpp`
- `Telegram/SourceFiles/ui/chat/attach/attach_bot_downloads.cpp`
- plus bulk source updates for RoundButton text-transform API usage.

## M2 (Android Parity)

Status: **Not Advanced in this pass**

- No Android build/test execution in this checkpoint.
- Android parity remains pending after Linux clean-build milestone passes.

## M3 (Full Feature Parity + Verification)

Status: **Not Started**

- Blocked on M1 clean Linux build and M2 validation.
- Full parity verification matrix and sign-off still pending.

## Notes

- Existing unrelated dirty submodule states (for example under `Telegram/lib_base` and `cmake`) were intentionally not altered in this checkpoint.
- This update reflects implementation progress only; it is not a completion declaration.
